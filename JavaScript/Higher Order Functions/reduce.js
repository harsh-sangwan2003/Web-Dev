const arr = [2, 3, 4, 5, 6];

let sum = arr.reduce((acc, ele) => acc + ele, acc = 0);
console.log(sum);

let multiplication = arr.reduce((acc, ele) => acc * ele, acc = 1);
console.log(multiplication);